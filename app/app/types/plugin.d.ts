type PluginSetting = {
    id: string
    name: string
}

type PluginInstanceSetting = {
    id: string
    name: string,
    width: number,
    height: number,
    x: number,
    y: number,
    resizable: boolean,
    alwaysOnTop: boolean,
}